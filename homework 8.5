#include <stdio.h>
#include <stdlib.h>
int main ()
{
	int n,*p,m=0,i,j,temp,q=0;
	printf("输入人数\n");
	scanf("%d",&n);
	p=(int*)malloc(n*sizeof(int));
	
	for(i=0;i<n;i++)
	{
		p[i]=i+1;
	}
	
	next:for(i=0;i<n;i++)
	{
		if(p[i]!=0)
		{
		  q=q+1;
		  if(q%3==0)
		  {
			  p[i]=0;
		  }
		}
	}
		
	//检查还剩多少人 
	for(i=0;i<n;i++)
	{
		if(p[i]!=0)
		m=m+1;
	}
	if(m==1)
	{
		for(i=0;i<n;i++)
		{
			if(p[i]!=0)
			temp=i+1;
		}
		printf("the number is %d",temp);
		free(p); 
		system("pause"); 
		return 0; 
	}
	else 
	{
		m=0;
	    goto next;
	}	
} 
